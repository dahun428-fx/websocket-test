# 소스 코드 리뷰

- 리뷰 일자: 2026-07-19
- 대상: `src/`, `public/index.html`, 프로젝트 설정 및 테스트
- 검증: `npm run typecheck`, `npm test` 통과 (14개 파일, 56개 테스트)
- 참고: 리뷰 시점의 미커밋 변경인 `src/types/auth.ts`, `src/schemas/signupSchema.ts`는 수정하지 않았다.

## 요약

즉시 기능을 깨뜨리는 컴파일 오류나 실패 테스트는 없었다. 다만 외부 입력에 대한 자원 제한과 로그인 방어가 없어 공개 환경에서는 서비스 거부 및 무차별 대입 공격에 취약하다. WebSocket 등록 흐름에는 비동기 작업 실패 시 인증·방 입장 상태가 일부만 반영되는 일관성 문제도 있다.

## 발견 사항

### [높음] 1. HTTP 및 WebSocket 입력 크기가 제한되지 않는다

근거:

- `src/server.ts:49-58`은 요청 본문을 제한 없이 문자열에 계속 누적한다.
- `src/server.ts:173`의 `WebSocketServer`에는 `maxPayload`가 설정되지 않아 `ws`의 큰 기본 허용량에 의존한다.
- 로그인 본문과 WebSocket 메시지는 완전히 수신된 뒤에야 JSON/Zod 검증을 거친다.

영향:

공격자가 매우 큰 요청을 동시에 보내면 메모리와 이벤트 루프를 소모시켜 프로세스를 중단시킬 수 있다. 로그인 JSON 자체는 작아야 하므로 큰 본문을 받아야 할 이유가 없다.

권장 수정:

- HTTP 본문에 작은 상한(예: 8~16 KiB)을 두고 초과 시 즉시 읽기를 중단한 뒤 `413 Payload Too Large`를 반환한다.
- `new WebSocketServer({ server, maxPayload: ... })`로 채팅 프로토콜에 맞는 상한을 명시한다.
- 제한 직전 크기는 허용되고 제한 초과 크기는 거부되는 통합 테스트를 추가한다.

### [높음] 2. 로그인 시도 제한이 없어 계정 공격과 CPU 고갈에 노출된다

근거:

- `src/server.ts:136-145`의 `/login` 경로에는 IP 또는 계정 기준 시도 제한이 없다.
- `src/service/authService.ts:24`는 존재하는 사용자에 대한 매 요청마다 비용이 큰 bcrypt 검증을 수행한다.
- 실패 횟수, 지연, 잠금, 감사 로그가 없다.

영향:

비밀번호 무차별 대입이 가능하고, 다수의 잘못된 비밀번호 요청만으로 bcrypt 작업을 집중시켜 CPU를 고갈시킬 수 있다.

권장 수정:

- 역방향 프록시 또는 애플리케이션 계층에서 IP와 사용자 ID를 함께 고려한 rate limit을 적용한다.
- `429 Too Many Requests`와 `Retry-After`를 반환하고, 성공/실패/제한 동작을 시간에 독립적인 테스트로 검증한다.
- 프록시 뒤에서 실행한다면 신뢰할 프록시 범위를 먼저 고정해 전달 헤더 위조를 막는다.

### [중간] 3. 로그인 응답 시간으로 사용자 존재 여부를 추정할 수 있다

근거:

- `src/service/authService.ts:20-22`는 사용자가 없으면 즉시 `null`을 반환한다.
- 사용자가 있으면 `src/service/authService.ts:24`에서 bcrypt 비교를 수행하므로 실패 경로의 계산 비용이 크게 다르다.

영향:

응답을 반복 측정하면 등록된 사용자 ID를 추정할 수 있다. 응답 메시지는 동일하지만 시간 차이가 계정 열거 신호가 된다.

권장 수정:

- 사용자가 없는 경우에도 고정된 유효 bcrypt 해시와 비교해 두 실패 경로의 비용을 비슷하게 만든다.
- 단위 테스트에서는 구현 호출 여부를 확인하고, 실제 시간 임계값에 의존하는 불안정한 테스트는 피한다.

### [중간] 4. WebSocket 등록 도중 실패하면 부분 등록 상태가 남는다

근거:

- `src/handlers/registerHandler.ts:40-43`에서 방과 사용자 상태를 먼저 변경한다.
- 이후 `src/handlers/registerHandler.ts:51-61`에서 등록 성공 응답과 DB 기반 히스토리 전송을 기다린다.
- `sendJson` 또는 `sendRoomHistory`가 실패하면 예외가 상위로 전달되지만 `ws.userId`, `ws.nickname`, `ws.room_id`는 복구되지 않는다.

영향:

클라이언트는 내부 오류를 받았는데 서버에서는 이미 등록된 상태가 될 수 있다. 재시도하면 `ALREADY_REGISTERED`가 발생하고, 실패 시점에 따라 성공 응답만 받은 뒤 입장 브로드캐스트는 누락될 수도 있다.

권장 수정:

- 등록을 하나의 상태 전이로 취급해 후속 작업 실패 시 이전 상태로 롤백하거나 연결을 닫는다.
- 히스토리 조회를 상태 변경 전에 수행하는 방식도 가능하지만, 조회와 실제 입장 사이의 순서 요구사항을 명시해야 한다.
- `sendRoomHistory` 실패 후 소켓 상태와 재시도 결과를 검증하는 테스트를 추가한다.

### [중간] 5. 닉네임의 신뢰 출처가 두 군데다

근거:

- `src/types/auth.ts:3-4`와 `src/auth/tokenService.ts:17`에서 JWT 닉네임이 선택 사항이다.
- `src/schemas/clientMessageSchema.ts:6-10`은 클라이언트가 닉네임을 반드시 보내도록 요구한다.
- `src/handlers/registerHandler.ts:39`는 토큰에 닉네임이 없으면 클라이언트 값을 신뢰한다.

영향:

현재 로그인으로 발급한 토큰은 닉네임을 포함하지만, 다른 발급 경로나 이전 토큰이 `sub`만 포함하면 사용자가 임의 닉네임을 선택할 수 있다. 인증된 표시 이름이라는 보장이 발급 경로에 따라 달라진다.

권장 수정:

- 닉네임을 JWT의 필수 클레임으로 만들고 WebSocket 등록 메시지에서는 제거하거나, 등록 시 사용자 저장소에서 최신 닉네임을 조회한다.
- 토큰에 닉네임이 없는 경우를 명시적으로 거부하는 테스트를 추가한다.

### [낮음] 6. DB 초기화가 동시 호출 및 중간 실패에 안전하지 않다

근거:

- `src/database/database.ts:16-25`는 전역 `database`가 비어 있는지 확인한 후 연결을 열어 대입한다.
- 동시에 두 번 호출되면 두 연결이 열릴 수 있고, 먼저 열린 연결이 전역 참조에서 사라질 수 있다.
- `src/database/database.ts:27-52`의 스키마 생성이 실패해도 열린 연결과 전역 참조가 함수 내부에서 정리되지 않는다.

영향:

현재 서버 시작 경로는 한 번만 호출하므로 발생 가능성은 낮다. 하지만 테스트 러너, 임베딩 코드 또는 향후 초기화 경로가 늘면 연결 누수나 부분 초기화 상태가 생길 수 있다.

권장 수정:

- 초기화 중인 `Promise`를 공유하고, 스키마 준비가 끝난 뒤에만 완성된 연결을 공개한다.
- 실패 시 해당 연결을 닫고 전역 상태를 초기화한다.
- 동시 초기화와 스키마 초기화 실패 후 재시도 테스트를 추가한다.

### [낮음] 7. 데모 자격 증명이 UI와 seed 스크립트에 고정되어 있다

근거:

- `public/index.html:192`에 `test1234`가 비밀번호 입력값으로 미리 채워져 있다.
- `src/scripts/seedUsers.ts:37-39`는 알려진 동일 비밀번호로 사용자를 생성한다.

영향:

로컬 데모 용도라면 편리하지만, 이 구성이 배포 환경에 그대로 노출되면 누구나 기본 계정으로 로그인할 수 있다.

권장 수정:

- 데모 전용임을 실행 환경으로 분리하고 운영 빌드에서는 기본값을 제거한다.
- seed 비밀번호를 환경 변수로 받거나 일회성 무작위 값으로 생성한다.

## 테스트 공백

현재 테스트는 정상 로그인, 잘못된 자격 증명, 잘못된 JSON, JWT 검증, 메시지 처리와 저장소 기본 동작을 잘 다룬다. 우선 추가할 테스트는 다음과 같다.

1. HTTP/WebSocket 최대 payload 경계 테스트
2. 로그인 rate limit 통합 테스트
3. 존재하지 않는 사용자도 dummy bcrypt 검증을 거치는 단위 테스트
4. 히스토리 전송 실패 시 WebSocket 등록 상태 롤백 테스트
5. 닉네임 없는 JWT 거부 테스트
6. DB 동시 초기화 및 초기화 실패 복구 테스트

## 권장 수정 순서

1. 외부 입력 크기 제한과 로그인 rate limit
2. 로그인 타이밍 차이 제거
3. WebSocket 등록 상태 전이 원자성 확보
4. 닉네임 신뢰 출처 단일화
5. DB 초기화 견고성 및 데모 자격 증명 분리
